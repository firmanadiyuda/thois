<%= button_to @path, method: @method, class:"bg-gradient-to-r hover:bg-gradient-to-br transition-all easy-in-out duration-300 focus:ring-4 focus:outline-none shadow-lg font-medium rounded-lg text-sm text-center #{@class}", data: (@show_spinner ? { turbo_submits_with: '<div class="ri-refresh-line animate-spin"></div>' } : {}).merge(@confirm.present? ? { turbo_confirm: @confirm } : {}) do %>
  <%= content %>
<% end%>