<div class="flex-col">
  <%= @label %>
  <% @options.each do |value, text| %>
    <div>
      <% id = "#{@name}_#{value}" %>
      <%= @form.radio_button @name, value %>
      <%= @form.label id, text %>
    </div>
  <% end %>
</div>  