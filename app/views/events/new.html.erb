<% content_for :title, "New event" %>

  <div class="grid grid-cols-2">
    <div class="">
      <div class="text-4xl"><b>New event</b></div>
    </div>
    <div class="text-right">
    </div>
  </div>

  <div class="w-full text-neutral-500 italic">
    <%= link_to "Events", events_path %> / New event
  </div>

<%= render "form", event: @event %>